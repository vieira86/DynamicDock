# Web Framework
fastapi>=0.68.0
uvicorn>=0.15.0
python-multipart>=0.0.5
pydantic>=1.8.0

# Molecular Libraries
biopython>=1.79
rdkit>=2022.9.3
numpy>=1.21.0
# NOTE: Open Babel is used as an external command-line tool (obabel), not
# imported in Python, so it's intentionally NOT listed here as a hard
# requirement - if it fails to install, the rest of setup shouldn't be
# blocked. `python setup.py` installs it automatically (via the
# 'openbabel-wheel' package, which has prebuilt binaries for Windows,
# macOS and Linux) as its own best-effort step. See README.md for manual
# install instructions if that doesn't work for your platform.

# Utilitários
python-dotenv>=0.19.0
requests>=2.26.0

# CORS
starlette>=0.14.2

# Tipos
typing-extensions>=3.10.0

# Desenvolvimento
pytest>=6.2.5
black>=21.7b0
flake8>=3.9.0
